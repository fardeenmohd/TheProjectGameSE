# welcome to the most useless library of 2017!
# original goal was to have a nice, cozy place for implementation of  Manhattan distance for tuples (or lists)
# who knows what other methods will end up here!

def Manhattan_Distance(a, b):
    return abs(a[0] - b[0]) + abs(a[1] - b[1])
